# NextAuth.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-key-here

# Database Configuration  
DATABASE_URL="postgresql://username:password@localhost:5432/daily_productivity"

# Google OAuth Provider
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth Provider  
GITHUB_ID=your-github-client-id
GITHUB_SECRET=your-github-client-secret

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace all placeholder values with your actual credentials
# 3. For production, set these in your Vercel dashboard under Settings > Environment Variables

# To get Google OAuth credentials:
# 1. Go to https://console.developers.google.com/
# 2. Create a new project or select existing
# 3. Enable Google+ API
# 4. Create OAuth 2.0 credentials
# 5. Set authorized redirect URIs to: http://localhost:3000/api/auth/callback/google (local) and https://yourdomain.com/api/auth/callback/google (production)

# To get GitHub OAuth credentials:
# 1. Go to https://github.com/settings/developers
# 2. Create a new OAuth App
# 3. Set Authorization callback URL to: http://localhost:3000/api/auth/callback/github (local) and https://yourdomain.com/api/auth/callback/github (production)

# For database, you can use:
# - Vercel Postgres (recommended for production)
# - Supabase
# - PlanetScale
# - Railway
# - Local PostgreSQL for development
